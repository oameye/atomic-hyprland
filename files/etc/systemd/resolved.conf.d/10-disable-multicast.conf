# systemd-resolved's built-in mDNS conflicts with avahi-daemon for .local
# discovery. avahi handles it better for CUPS network printer discovery.
# Mirrors omarchy install/config/hardware/printer.sh.
[Resolve]
MulticastDNS=no
