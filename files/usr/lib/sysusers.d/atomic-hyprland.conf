# Declaratively create the groups atomic-hyprland-dx-groups needs.
# systemd-sysusers runs this on boot (or at image build when packages
# provide sysusers.d snippets), so /etc/group gets populated without
# the dx-groups script having to copy entries from /usr/lib/group.
g docker -
g incus-admin -
g libvirt -
