# Installed by atomic-hyprland-flatpak-preinstall when the manifest changes.
# Parsed locally by /usr/bin/atomic-hyprland-flatpak-preinstall.
# Uninstallation is opt-in (user removes with `flatpak uninstall`).

[Flatpak Preinstall org.mozilla.Thunderbird]
Branch=stable
IsRuntime=false
